#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  
  int x;
  x = 0;
  while(x < 10)
  {
    puts("vou executando loop while!!");
    x = x + 1;
  }
  system("PAUSE");	
  return 0;
}
